---
import { useTranslations } from '../../i18n/';
import BaseLayout from '../../layouts/BaseLayout.astro';
import Navigation from '../../components/Navigation.astro';
import Footer from '../../components/Footer.astro';

const lang = 'fr';
const t = useTranslations(lang);
---

<BaseLayout 
    lang={lang} 
    title={`${t('features.title')} - ${t('site.title')}`}
    description={t('features.subtitle')}
>
    <Navigation />
    
    <!-- Features Header -->
    <section class="section hero">
        <div class="container text-center">
            <h1 class="mb-6">{t('features.title')}</h1>
            <p class="hero-description text-secondary max-w-3xl mb-8">{t('features.subtitle')}</p>
        </div>
    </section>

    <!-- Features Grid -->
    <section class="section features">
        <div class="container">
            <div class="grid grid-md-2 grid-lg-3">
                <div class="card">
                    <div class="feature-icon mb-4">💰</div>
                    <h3 class="mb-4">{t('features.rent.title')}</h3>
                    <p class="text-secondary">{t('features.rent.description')}</p>
                </div>
                <div class="card">
                    <div class="feature-icon mb-4">👥</div>
                    <h3 class="mb-4">{t('features.collaboration.title')}</h3>
                    <p class="text-secondary">{t('features.collaboration.description')}</p>
                </div>
                <div class="card">
                    <div class="feature-icon mb-4">📊</div>
                    <h3 class="mb-4">{t('features.expenses.title')}</h3>
                    <p class="text-secondary">{t('features.expenses.description')}</p>
                </div>
                <div class="card">
                    <div class="feature-icon mb-4">🛡️</div>
                    <h3 class="mb-4">{t('features.trust.title')}</h3>
                    <p class="text-secondary">{t('features.trust.description')}</p>
                </div>
                <div class="card">
                    <div class="feature-icon mb-4">📱</div>
                    <h3 class="mb-4">{t('features.mobile.title')}</h3>
                    <p class="text-secondary">{t('features.mobile.description')}</p>
                </div>
            </div>
        </div>
    </section>

    <Footer />
</BaseLayout>

<style>
    .hero {
        padding-top: 2rem;
    }

    @media (min-width: 768px) {
        .hero {
            padding-top: 3rem;
        }
    }

    .hero-description {
        font-size: clamp(1rem, 2vw, 1.25rem);
        line-height: 1.7;
    }

    .feature-icon {
        font-size: 2.5rem;
        line-height: 1;
    }

    .features {
        background-color: var(--bg-primary);
    }
</style>